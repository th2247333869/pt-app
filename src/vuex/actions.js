const actions = {
  increment : ({commit}) => commit('increment'),
  decrement : ({commit}) => commit('decrement'),
  setUserInformation:({commit}) => commit('setUserInformation'),
  setMenus:({commit}) => commit('setMenus'),
}

export default actions
